# Phase 3.0 Knowledge Review Data Model

Cross-project Knowledge Practice reuses the existing review tables. A graded Notion Graph node is stored as `item_id = Notion page id`, `item_kind = knowledge`. Because Graph learning overlay already matches review state by Notion node id, no additional mapping table is required.
