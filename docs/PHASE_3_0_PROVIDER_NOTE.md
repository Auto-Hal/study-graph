# Phase 3.0 Provider Note

Graph Practice prioritizes tracked items whose `due_at` has arrived, then fills the session with unseen eligible nodes. Future-due tracked nodes are not shown until their schedule arrives.
